<nav class="navbar navbar-dark bg-success px-3">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <div class="d-flex align-items-center gap-2">
      {{#if @showBack}}
        <button class="btn btn-outline-light btn-sm" {{action "goBack"}}>
          ← Back
        </button>
      {{/if}}

      <span class="navbar-brand mb-0 h1">
        {{@title}}
      </span>
    </div>

    <div class="d-flex align-items-center gap-3">

      {{#if this.session.user}}
        <div class="text-end text-white">
          <div class="fw-semibold">
            {{this.session.user.name}}
          </div>
          <div class="small opacity-75">
            {{this.session.user.registerNumber}}
          </div>
        </div>
      {{/if}}

      <button
        class="btn btn-outline-light btn-sm"disabled={{this.isLoggingOut}} {{action "logout"}}>
        {{if this.isLoggingOut "Logging out..." "Logout"}}
      </button>

    </div>

  </div>
</nav>
