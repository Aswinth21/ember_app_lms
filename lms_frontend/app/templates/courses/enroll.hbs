<div class="container my-5 d-flex justify-content-center">
  <div class="card p-4 shadow-sm" style="max-width:420px">

    <h4>{{model.name}}</h4>
    <p class="text-muted">{{model.description}}</p>

    <div class="text-end">
      <button class="btn btn-success" {{action "enroll" model}}>
        Request Enroll
      </button>
    </div>

  </div>
</div>
